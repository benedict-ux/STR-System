# Product

STR Monitoring System — a single-page web form for tracking Stock Transfer Requests (STRs) across store locations.

Users enter an STR number, date, and store, then add product line items with quantities. Submitting generates records in an on-page table with status tracking (Pending, Returned, Paid, Cancelled).

Stores: Matalino, Ankam, Kamuning.
